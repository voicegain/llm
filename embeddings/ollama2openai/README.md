This is a python webservice that accepts embeding requests in OpenAI style and passes them to [ollama-style embedding service](https://ollama.com/blog/embedding-models)

Requires:
```
pip install Flask requests
```
